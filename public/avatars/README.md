# Avatars onboarding

Dépose ici `avatar-01.png` … `avatar-12.png` (illustrations 256×256).

En attendant, des placeholders SVG (`avatar-01.svg` …) sont référencés par le code.
Quand tes PNG sont prêts, remplace les chemins dans `lib/onboarding/parcours.ts` (`AVATAR_PRESETS`) pour pointer vers `.png`.
