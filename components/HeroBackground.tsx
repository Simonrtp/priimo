// === HERO BACKGROUND ===
// Fond statique (dégradés CSS) — pas de suivi souris ni boucle rAF.

export default function HeroBackground() {
  return (
    <div aria-hidden className="hero-bg">
      <div className="hero-grid" />
    </div>
  );
}
